[ req ]
default_bits = 4096
prompt = no
default_md = sha256
distinguished_name = dn
req_extensions = req_ext

[ dn ]
CN = nexus.local

[ req_ext ]
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = nexus.local
DNS.2 = nexus-docker.local
DNS.3 = host.docker.internal
